version: 2.1
orbs:
    node: circleci/node@3.0.0
jobs:
    build-and-test:
        executor:
            name: node/12
        steps:
            - checkout
            - node/with-cache:
                  steps:
                      - run: yarn build:ci
                      - run: yarn test:ci
workflows:
    build-and-test:
        jobs:
            - build-and-test
