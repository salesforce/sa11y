{
    "extends": "../../tsconfig.common.json",
    "compilerOptions": {
        "rootDir": "src",
        "outDir": "dist",
        "types": ["node", "webdriverio/async"]

    },
    "include": ["src"],
    // "@sa11y/common" is the base package and should not depend on any 
    //  other sa11y packages to prevent cyclic dependency
}
