{
    "packages": ["packages/*"],
    "version": "independent",
    "exact": true,
    "npmClient": "yarn",
    "useWorkspaces": true,
    "command": {
        "version": {
            "allowBranch": "release",
            "message": "chore(release): bump versions"
        }
    }
}
